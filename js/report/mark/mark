1.loaderManager改造，支持多个模型文件先后导入场景，支持ply，wrl，stl，obj格式模型其中任意一种
2.window.resize做成对外接口，支持调用该接口随意调整canvas视窗
3.navigator导航模块重写，导航块必须在相机对象内，旋转作为回调传给control.update()
4.初始化场景的相机位置调整，检查模型导入是否以场景包围盒中心为target，注意每次导入一个模型进来要重新计算包围盒设置相机
5.增加相机回到初始位置功能fit
6.将需要调用的接口开放出来，其他混淆掉